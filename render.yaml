services:
  - type: web
    name: inventarization
    runtime: static
    buildCommand: node scripts/generate-config.mjs
    staticPublishPath: .
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false
